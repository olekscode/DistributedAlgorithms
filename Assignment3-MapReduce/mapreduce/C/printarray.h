#ifndef PRINTARRAY_H
#define PRINTARRAY_H

#include <stdio.h>
#include <stdlib.h>
#include "mapreduce.h"

void print_array(int* arr, size_t size);
void print_dict(struct Pair* pairs, size_t size);

#endif // PRINTARRAY_H